export type MyUploadedRecipeProps = {
    recipeSeq: number,
    recipeTitle: string,
    recipeContent: string,
    recipeBigCategory: string,
    recipeSmallCategory: string,
    recipeRating: number,
    recipeReadcount: number,
    recipeThumbnail: string,
    memberId:string
}